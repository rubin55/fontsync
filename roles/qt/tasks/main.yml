- name: "Check if dependent variables are defined"
  ansible.builtin.assert:
    that:
      - font_size_mono is defined
      - font_size_sans is defined
      - font_size_ratio_qt_mono is defined
      - font_size_ratio_qt_sans is defined

- name: Configuring qt
  ansible.builtin.debug:
    msg: "Placeholder role: qt"
